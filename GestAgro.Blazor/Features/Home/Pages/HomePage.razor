@page "/"

<div class="spa-container fade-in">
    
        <section class="hero-content-limit text-center">
        
            <span class="badge rounded-pill px-3 py-2 mb-3 border border-success text-success bg-success bg-opacity-10 shadow-glass">
                Em Breve: AgroGest 1.0
            </span>
        
            <h1 class="hero-title p-2">
                Pare de comprar insumos<br>no preço errado.
            </h1>
        
            <p class="lead text-muted mb-4 mx-auto fw-medium">
                A primeira plataforma que monitora o mercado agrícola 24/7 e avisa o momento exato para fechar negócio. Maximize sua margem antes mesmo de plantar.
            </p>

            <div class="d-flex gap-3 justify-content-center">
                <button class="glass-button btn-lg rounded-pill px-5 py-3 fw-bold bg-success text-white border-0 shadow-lg hover-scale" @onclick="ScrollToForm">
                    Quero Acesso Antecipado
                </button>
                <button class="glass-button btn-lg rounded-pill px-4 py-3 text-muted border-0 hover-scale">
                    <i class="bi bi-play-circle me-2"></i> Ver como funciona
                </button>
            </div>

        </section>


    <section class="demo-card component-glass shadow-glass p-5 rounded-4 my-4 mx-auto" style="max-width: 700px;">
        
        <div class="floating-badge badge-alert">
            <i class="bi bi-exclamation-triangle-fill"></i>
            <span>Alta de 23,3%</span>
        </div>

        <div class="text-center mb-4">
            <p class="small text-uppercase fw-bold text-muted mb-2">Simulação</p>
            <h3 class="fw-bold" style="color: var(--palette-dark-green)">Alerta de Preço: Ureia</h3>
        </div>

        <div class="row g-3 text-center">
            <div class="col-6 border-end border-secondary border-opacity-25">
                <p class="mb-1 small text-muted">Preço Médio (Semestre passado)</p>
                <h4 class="text-decoration-line-through text-muted">US$ 401,5</h4>
            </div>
            <div class="col-6">
                <p class="mb-1 small text-muted fw-bold text-success">Preço Agora</p>
                <h4 class="fw-bold text-success">US$ 495,0</h4>
            </div>
        </div>

        <div class="mt-4 p-3 rounded-3 bg-success bg-opacity-10 border border-success border-opacity-25 d-flex align-items-center gap-3">
            <i class="bi bi-check-circle-fill text-success fs-4"></i>
            <div class="text-start">
                <span class="fw-bold d-block text-dark">Recomendação de Compra</span>
                <small class="text-muted">O algoritmo detectou um aumento massivo de valores referentes a fertilizantes, Recomenda-se aguardar estabilidade do mercado.</small>
            </div>
        </div>
    </section>

    <section class="py-4">
        <div class="row g-4 justify-content-center">
        
            <div class="col-md-4 col-sm-6">
                <div class="benefit-card text-center shadow-glass">
                    <div class="feature-icon bg-success bg-opacity-10 text-success border border-success border-opacity-25">
                        <i class="bi bi-graph-up-arrow"></i>
                    </div>
                    <h5 class="fw-bold mb-2">Monitoramento Real</h5>
                    <p class="text-muted small mb-0">
                        Conectado 24h às principais bolsas de commodities (CBOT e B3).
                    </p>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="benefit-card text-center shadow-glass">
                    <div class="feature-icon bg-warning bg-opacity-10 text-warning border border-warning border-opacity-25">
                        <i class="bi bi-whatsapp"></i>
                    </div>
                    <h5 class="fw-bold mb-2">Alertas WhatsApp</h5>
                    <p class="text-muted small mb-0">
                        Não precisa abrir o site. Receba o aviso de compra no celular.
                    </p>
                </div>
            </div>

            <div class="col-md-4 col-sm-6">
                <div class="benefit-card text-center shadow-glass">
                    <div class="feature-icon bg-primary bg-opacity-10 text-primary border border-primary border-opacity-25">
                        <i class="bi bi-shield-check"></i>
                    </div>
                    <h5 class="fw-bold mb-2">Compra Segura</h5>
                    <p class="text-muted small mb-0">
                        Indicação apenas de fornecedores verificados e com reputação.
                    </p>
                </div>
            </div>

        </div>
    </section>

    <section id="register-section" class="component-glass p-5 rounded-4 text-center mx-auto shadow-glass" style="max-width: 600px; border-top: 4px solid var(--palette-lime-bright);">
        
        <h3 class="fw-bold mb-3">Faça seu Pré-registro</h3>
        <p class="text-muted mb-4">Estamos validando a ideia para iniciar o desenvolvimento. Preencha para entrar na lista.</p>

        @if (formSubmitted)
        {
            <div class="alert alert-success d-flex align-items-center fade-in" role="alert">
                <i class="bi bi-check-circle-fill me-2 fs-4"></i>
                <div>
                    <strong>Sucesso!</strong> Você está na lista. Entraremos em contato em breve.
                </div>
            </div>
        }
        else
        {
            <form @onsubmit="HandleSubmit">
                <div class="mb-3 text-start">
                    <label class="form-label small fw-bold ms-1">Seu Nome</label>
                    <input class="form-control glass-input rounded-3 p-3" placeholder="Ex: João da Silva" required />
                </div>
                
                <div class="mb-3 text-start">
                    <label class="form-label small fw-bold ms-1">WhatsApp</label>
                    <input class="form-control glass-input rounded-3 p-3" placeholder="(00) 90000-0000" required />
                </div>

                <div class="mb-4 text-start">
                    <label class="form-label small fw-bold ms-1">Principal Cultura</label>
                    <select class="form-select glass-input rounded-3 p-3 text-muted">
                        <option selected>Selecione...</option>
                        <option>Soja</option>
                        <option>Milho</option>
                        <option>Algodão</option>
                        <option>Pecuária</option>
                    </select>
                </div>

                <button type="submit" class="glass-button w-100 py-3 rounded-3 fw-bold bg-dark text-white border-0 hover-scale">
                    Entrar na Lista de Espera
                </button>
                
                <p class="mt-3 mb-0 small text-muted">
                    <i class="bi bi-lock-fill me-1"></i> Seus dados estão seguros. Sem spam.
                </p>
            </form>
        }
    </section>

</div>

@code {
    private bool formSubmitted = false;

    private async Task ScrollToForm()
    {
        // Em Blazor puro, a rolagem suave requer JS Interop, 
        // mas aqui podemos simular ou deixar o utilizador rolar.
        // Idealmente injetaria IJSRuntime para fazer: document.getElementById('register-section').scrollIntoView();
        await Task.Delay(100); 
    }

    private async Task HandleSubmit()
    {
        // Simula envio para API
        await Task.Delay(1000);
        formSubmitted = true;
    }
}